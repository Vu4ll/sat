<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
        <%= title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" href="images/money-bills.svg" type="image/x-icon">
    <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v6.7.2/css/all.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        html,
        body {
            height: 100%;
        }

        body {
            display: flex;
            flex-direction: column;
        }

        .content {
            flex: 1;
        }

        footer section a {
            font-size: 2rem;
        }

        .custom-bg-dark {
            background-color: #2e2c2c !important;
        }

        #themeToggle {
            cursor: pointer;
        }
    </style>
</head>

<body class="d-flex flex-column">
    <%- include("partials/navbar") %>

    <div class="container mt-4 content">
        <% if (messages.error) { %>
            <div class="d-flex justify-content-center">
                <div class="alert alert-danger text-center p-2 rounded-3 shadow-sm w-50">
                    <%= messages.error %>
                </div>
            </div>
        <% } %>

        <%- body %>
    </div>

    <br><br>
    
    <%- include("partials/footer") %>

    <script>
        document.cookie = "timezone=" + Intl.DateTimeFormat().resolvedOptions().timeZone + "; path=/";
        document.cookie = "locale=" + navigator.language + "; path=/";
    </script>
    <script src="/js/darkmode.js"></script>
</body>
</html>
