<div class="container mt-5">
    <form action="/reset-password" method="POST" class="w-50 mx-auto mt-3">
        <h2 class="mt-4">Yeni Şifre Belirleyin</h2>
        <p>Lütfen yeni şifrenizi girin:</p>

        <input type="hidden" name="token" value="<%= token %>">
        <div class="mb-3">
            <label class="form-label">Yeni şifre</label>
            <input type="password" class="form-control" name="newPassword" placeholder="Yeni şifrenizi giriniz"
                required>
        </div>

        <div class="mb-3">
            <label class="form-label">Yeni şifre tekrar</label>
            <input type="password" class="form-control" name="confirmNewPassword"
                placeholder="Yeni şifrenizi tekrar giriniz" required>
        </div>

        <button type="submit" class="btn btn-success">Şifreyi Güncelle</button>
    </form>
</div>